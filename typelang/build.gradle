task runInterpreter(type: JavaExec) {
  classpath sourceSets.main.runtimeClasspath
}

run {
   standardInput = System.in
}

mainClassName="typelang.Interpreter"

generateGrammarSource {
    // arguments += ["-no-listener", "-package", "typelang.parser; import static typelang.AST.*; import typelang.*; import typelang.Type.*"]
}
